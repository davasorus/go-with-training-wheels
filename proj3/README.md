# Project 3: Multi-Module Workspace
This project demonstrates the use of Go Workspaces (`go.work`) to manage multiple modules simultaneously.

## Structure
The `workspace` directory contains several submodules that are integrated using a `go.work` file.
