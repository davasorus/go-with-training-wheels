# Project 2: Quote Fetcher
A Go application that uses the `rsc.io/quote` package to display a quote in the terminal.

## Getting Started
To run this project, navigate to the directory and execute:
```bash
go run quote.go
```
